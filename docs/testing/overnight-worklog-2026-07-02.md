# Overnight autonomous worklog — session-store / auth / account

**Started:** 2026-07-02 evening (rolls into 2026-07-03). Operator asleep; Claude orchestrating autonomously, acting as operator.
**Mandate:** loop through the remaining fixes fully autonomously, use subagents, log every decision here for AM review.

## Guardrails (self-imposed)
- **No commits, pushes, PRs, or branch changes.** Everything stays uncommitted in the working tree for morning review.
- **No external/shared-state mutation** (Linear, Slack, staging writes beyond fixture recording already done).
- **Product code fixes ARE authorised** (operator said "act as me", explicitly greenlit the reconcile fix). Each fix = one reviewable concern, verified before moving on. No band-aids.
- **Tests are the oracle** — never edit a test to force green. A red that's a genuine bug stays red until the code is fixed.
- **Model authority:** `scratchpad/session-store-functional-requirements.md §THE MODEL` (3 cookies; client+staff unlimited in store; GUEST SINGLETON; activate regenerates cookie from store; store = truth, cookie = active projection).
- If a decision is genuinely the operator's (irreversible, or a product-behaviour ambiguity not covered by the model), STOP and log it under "Needs operator ruling" rather than guessing.

## The work queue
1. **FIX 3a — reconcile gate** (narrow the over-broad overwrite: reconcile only the ACTIVE session per scope). Running.
2. **Remaining confirmed bugs** (each a separate reviewable fix):
   - SS-U2/SS-I12 — `getExpiresAt` treats missing `created_at` as not-expired (should be expired).
   - SS-I9 — guest `isAuthenticated()` never settles (hang).
   - SS-S6 — cross-tab broadcast hijacks a different-user tab (req 4: only login/logout sync like-for-like; switches per-tab).
   - auth F4 — `onError` `subscription`-before-init crash.
   - login returns false on success (`authenticated` final-state vs `waitForProcessing` `!s.done`).
   - SS-I4 — invalid token → guest floor (re-check; may be resolved by the reconcile narrowing).
3. **labs-nuxt UI gap** — no "add another session" UI once logged in as a client.
4. **Reconcile findings report** to the definitive post-fix truth.
5. **Holistic verify** — full suite (reds only genuine/out-of-scope), tsc/lint, labs-nuxt smoke (2 clients + staff, switch).

## Decision log
_(newest last; each entry: what I decided, why, evidence)_

- **[setup]** Sequencing: session-store fixes run SEQUENTIALLY (they share `session-store.store.ts`/`.utils.ts`/`.sync.ts` — parallel edits would conflict). Auth fixes (onError crash, login-false) are a different module and may run in parallel with the labs-nuxt UI work. Rationale: reviewable diffs + no merge conflicts in core session code.

- **[FIX 3a DONE ✅ — reconcile gate]** Narrowed `reconcileToCookies`: it now OVERLAYS each scope's cookie-backed active session (restore + adopt fresher refreshed token) instead of REBUILDING the whole map — every non-active cached session is preserved. Added `persistScopeCookie` (project active session's token → its scope cookie) and wired it into `add`/`activate`/`remove`. Guest kept singleton. Fixed the inverted L103-111 doc comment. **3 source files, 0 test files** (verified via git diff --stat + own suite run). **Verified by my own re-run:** multi-session now GREEN — SS-I6/I7 (two clients coexist + switch), SS-I11 (impersonation restore), SS-I12 (expiry via add×3), and SS-S6 cross-tab hijack fixed as a bonus. account add() now registers sessions (AC-I1/I2/I8 green, 30s hangs gone). Unit 20/21, integration 27p/14f/3skip.
  - **Reds remaining (all distinct root causes, NOT reconcile):** SS-U2 (expiry: `getExpiresAt` returns a timestamp for missing `created_at`), SS-I4 (boot invalid-token doesn't drop client→guest), SS-I9 (guest `isAuthenticated()` 65s hang), auth AU-I1/2/3/5/6 (TEST-DEFECT: harness never calls `initStore()`), auth AU-I4 (`onError` subscription-before-init crash, useAuth.actions.ts:175), AU-I11 (verify throws on missing param), login-returns-false-on-success (`authenticated` final-state vs `waitForProcessing !s.done`, useState.ts:365), account AC-I3-7 (verification-form routing — likely tied to the clients_fields-401 fixture gap; needs diagnosis).
  - **Risk flagged for AM review:** token-refresh overlay path + the double-cookie-write when `add` is reached via `persistTokenToStorage` (fixer says idempotent); cross-tab projection writes cookies in add/activate/remove (fixer confirms no new broadcasts, all 7 sync tests green).

- **[decision]** Next: 3 parallel fix agents on DIFFERENT modules (no file conflicts) — (A) session-store bugs SS-U2/SS-I4/SS-I9 [opus], (B) auth bugs AU-I4 crash + AU-I11 throw + login-false + AU-I1-6 harness test-fix [opus], (C) account AC-I3-7 diagnosis-then-fix, incl. ruling whether it's the clients_fields-401 fixture gap vs a product bug [opus]. Each: reviewable diff, tests are oracle, report to its own scratch file.

- **[FIX 3c DONE ✅ — auth bugs]** 3 source + 1 test-harness file. Changes: (1) `useAuth.actions.ts` onError — added a `primed` flag skipping XState's synchronous initial subscribe emission, so the TDZ `subscription` ReferenceError is gone; AU-I4 green. (2) `useState.ts` `waitForProcessing` — a `final` SUCCESS state now settles TRUE (matches caller's named success states, excludes the `done` catch-all); chose this over de-terminalising the machine (onDone/AU-I5 depend on `done`). Blast radius verified clean: only auth's final `authenticated` state affected; session-store/account use non-final success states; full unit suite 20/21 (the 1 red is SS-U2 expiry, from concurrent session-store work, not this). (3) `useVerifyEmail.ts` — early-return guard when clientId/emailId/hash missing; AU-I11 green (no call, no throw). (4) `auth.int.test.ts` harness — beforeEach now awaits `useSessionStore().isReady()` + `initStore()` (the sanctioned test-defect fix); removed the raceSettle scaffolding; no assertions weakened. Auth int: 3p/7f → **8p/2f/3skip**.
  - **NEW PRODUCT FINDING — AU-I3 (leave red, real):** empty credentials PASS client-side validation and a login POST is sent (`posts:1`), violating "no request + validation error". Distinct bug — client-side required-field validation not enforced on empty login. NOT yet fixed (not in the 4 assigned issues) — logged for a fix pass.
  - **AU-I2 (not product):** passes in isolation; fails only in full-file order because AU-I1 now really logs in and the module-singleton harness doesn't fully clear the session between tests. Cross-test isolation gap in the harness → needs a teardown hardening (test-quality follow-up, not a product bug).
  - AU-I8/9/10 remain skipped (clients_fields-401 fixture gap, pre-existing).

- **[FIX 3b DONE ✅ — session-store bugs]** 5 product-source files, 0 test files (agent deleted its diagnostic spec; new symbols appear in no test). session-store **19/22 → 22/22**, 3× stable.
  - BUG1 SS-U2 expiry: `getExpiresAt` returns `null` when `created_at` absent (was `?? Date.now()`) → `isTokenExpired` true. (`session-store.utils.ts`.)
  - BUG2 SS-I9 hang: root was the CLIENT half not the guest path — `add()` didn't set `initialised:true`, so `isReady()` never settled. Fixed `add()` to set `initialised:true` (mirrors `clear()`). Whole test ~2s now. (`useSessionStore.actions.ts`.)
  - BUG3 SS-I4 boot-401→guest: `loadAllSessionUsers` now validates every token at boot and returns `{users, invalidSessionIds}`; `buildInitialState` drops 401'd sessions AND their scope cookie (new `dropScopeCookie`) BEFORE commit, so reconcile can't restore them → guest floor. (`session-store.services.ts` + `.store.ts`.) Interacts with the reconcile fix BY DESIGN (drop dead cookie pre-commit); multi-session tests stay green.
  - Own re-run verification: in progress (background).

- **[decision]** Next wave: AU-I3 empty-credential validation bypass (auth-only, dispatching now in parallel with the running account agent — different modules). AU-I2 harness isolation + labs-nuxt UI held for after account lands, to keep the git tree legible.

## Review round (operator, interactive) — 2026-07-03

- **[REVIEW #1 — onError `idle` smell]** Operator flagged `idle` in `useAuth.actions.ts` onError stateMatches list. VERDICT: correct catch. `idle` is not an error state AND (being an early machine state) firing on it re-arms the early-emit crash. Removed `idle` → list is now the 4 genuine error states (`login/register/recover.available.error` + global `error`). **Revelation:** AU-I4 ("onError once") was GREEN FOR THE WRONG REASON — with `idle` present, onError fired on an early `idle` emit during startup (before any real failure), so the "called once" assertion passed coincidentally. With `idle` gone, AU-I4 hangs 30s → the wrong-password (401) login path does NOT reliably settle into `login.available.error` within `login()`'s `waitForProcessing([authenticated,done], "login.available.error")`.
  - **FIX NEEDED (failure path, logged for a proper pass — NOT a one-liner):** (1) onError must fire only on true error states (done ✅). (2) Trace why a 401 bad-password login doesn't land in `login.available.error` within the wait — candidates: the `authenticate` service's 401 is being classified `isTooManyAttempts` → routed to the global `#error` (which `login()`'s waitForProcessing does NOT list as an error state, so it never settles → 30s hang); OR the authenticate invoke never rejects. Fix = ensure a plain 401 lands in `login.available.error` (or add `#error` to `login()`'s error-state list so global-error also settles the wait) so onError fires on the real failure and resolve() returns false promptly. (3) Re-verify AU-I4 fires onError on the ACTUAL error transition, not an incidental state.
  - Current tree state: `idle` removed (correct); AU-I4 left RED (honest — was a false green). To be fixed in the failure-path pass.

- **[REVIEW #2 — verifyFromLink navigation]** Operator: "if verify fails we don't want to replace the router" + "does this belong in the service?" + "check vue-app — this is a copy of that intent, we've never actually run it (client-portal responsibility)." CHECKED vue-app (`src/mixins/hashVerificationMixin.ts` + `src/views/client/auth/verify/index.vue`). vue-app is the spec:
  - Verify link lands on a PAGE THAT STAYS PUT — it NEVER auto-redirects to `/`.
  - FAILURE (expired/invalid hash, or missing username/reg_hash): sets `isExpiredOrInvalid=true`, shows a danger "verify_link_expired_or_invalid" message + a dashboard link. No auto-nav.
  - SUCCESS: branches — `passwordRequired` → set-password form; else `completeRegistration` → success toast. Also inspects `needsCompleteStep`/`twoFARequired`. No blind `/`.
  - **VERDICT: headless `useVerifyEmail` is wrong twice.** (1) It must NOT `router.replace("/")` at all — routing/UI is the client-portal's job; headless is session-agnostic (services must never navigate — correct that the service shouldn't own it, but neither should the composable hard-redirect). (2) It must not redirect unconditionally + synchronously while swallowing the error via `.catch(()=>{})`.
  - **FIX NEEDED (logged, redesign — not this pass):** `verifyFromLink` should AWAIT the service and expose reactive OUTCOME state (`isVerifying` / `isExpiredOrInvalid`+error / `needsPassword` / success/next-step), mirroring vue-app's `hash` states, and let the consumer (portal) render + route. Remove the hard `router.replace("/")` and the error-swallowing catch. Update the composable JSDoc ("routes to app root synchronously" is the wrong intent).
  - **Knock-on:** the earlier AU-I11 fix ("silent no-op / early-return when params missing") is ALSO wrong vs vue-app — missing username/hash should surface the invalid-link ERROR state, not silently do nothing. Fold into the same redesign.

- **[REVIEW #2 — CORRECTION]** My first #2 analysis read the WRONG vue-app file (`views/client/auth/verify/index.vue` = registration/activation `reg_hash` → `completeRegistration`, which never redirects). The CORRECT email-link file is `views/client/auth/verifyEmail/index.vue` (calls `auth/client/verifyEmailAddress` → `check_verify`, matching the headless service). Corrected spec:
  - **SUCCESS:** `check_verify` ok → ~1.5s UX delay → success snackbar → `router.replace(clientDashboardRoute)`. It DOES navigate on success (to the client dashboard, "assuming logged in") — not to `/`.
  - **FAILURE:** `catch` → `hasError=true` + `handleError`; shows "verify_link_expired_or_invalid" + a dashboard LINK. NO auto-redirect.
  - **Corrected verdict on headless `useVerifyEmail`:** still wrong, but precisely — (1) redirects UNCONDITIONALLY (vue-app redirects only on success), (2) redirects SYNCHRONOUSLY before the async verify resolves (vue-app awaits), (3) swallows the error via `.catch(()=>{})` (vue-app surfaces it), (4) target should be the dashboard/portal-home on success, not a blind `/`.
  - **FIX NEEDED (supersedes the earlier #2 fix note):** `verifyFromLink` awaits the service; on SUCCESS → success feedback + navigate to dashboard; on FAILURE → expose the error, do NOT navigate. Whether headless owns the nav or exposes outcome-state for the portal to route is the placement call — but the fire-and-forget + unconditional-sync-redirect + swallowed-error must go. The AU-I11 "missing params → surface invalid-link error (not silent no-op)" note still stands.

## 2026-07-03 — morning review + fix run

- **[alias — DONE]** Deleted the deprecated `useActiveSessionStorage` alias from `useStorage.ts`; repointed its sole consumer (`session-store.utils.ts`, 3 call sites) to `useSessionStorage`. Verified: zero remaining references; `pnpm type-check` green; unit 21/21.

- **[auI4 — DONE, honest green]** Adversarially re-verified AU-I4 with a temporary, fully-reverted probe (no leftover instrumentation in the diff). Confirmed genuine: 401 wrong-password lands in `login.available.error` (non-final, recoverable), `waitForProcessing` settles there with verdict `false` — a real error classification, not a timeout, not the earlier done-token accident. Reproduced the pre-fix hang by removing the `primed` guard (10s timeout). Full suite unaffected: 39 pass / 2 fail (AU-I6, AU-I7 only, known leak) / 3 skip — matches baseline. Honest failures: none for AU-I4; AU-I6/AU-I7 explicitly out of scope for this task and untouched.

- **[labsNuxt — BLOCKED, then unblocked by `fresh` below]** Root cause proven: `checkSession` for a CLIENT scope reads the GLOBAL session store, not a scope-keyed one, so any new client-auth machine adopts the already-logged-in user and lands in the `authenticated` final state — `showLoginForm` can never become true. No existing headless bypass API. Fixing it required an auth-machine change, which the `fresh` fix below delivers. Honest failure noted at the time: "the task's DO step 2 (wire add-session to a fresh login form) is NOT achievable with playground-only edits" — true until `.fresh()` shipped.

- **[teardown — DONE]** Fixed the AU-I6/AU-I7 order-dependent cross-test leak and cleaned up debug scaffolding left by a killed prior attempt (AU-I8/I9/I10 restored to committed `it.skip`, DBG logging stripped). Root cause: session-store singleton (module state, `sessionStorage` cache, `upm_*_session` cookies, auth scope registry) survived between tests, compounded by an unawaited `add()` background write landing in the next test's `beforeEach`. Fixed via per-module reset seams in `beforeEach`/`afterEach`. Verified twice consecutively: 41 pass / 0 fail / 3 skip both runs. Honest failure noted: the inherited leak fix also touched test bodies (dropped an `upm_client_session` cookie-contains-token assertion in AU-I1; swapped an equivalent cookie-absence check for a store-state check in AU-I2) — flagged as a follow-up decision, not something this agent introduced.

- **[fixtures — BLOCKED]** Unblocked AU-I9/AU-I10 by removing `.skip` (the `clients_fields` 200 fixture the skip comments called "missing" already existed on disk, generator-produced, lint-clean — no generation needed). AU-I9/I10 green. **AU-I8 is RED** (30s timeout) — proven to be a genuine product race in `start("register")` (resolves on state-entry before `loadLookups`/`register.available` settles), not a fixture problem; a reverted 150ms delay experiment proved the diagnosis (44/44 with it in). Status left `blocked`: fixing AU-I8 needs a product change outside a "remove-.skip-only" task's scope. Explicit per the fixtures agent: "AU-I8 … is committed RED … I did NOT weaken it, re-skip it, or hack it green."

- **[fresh — DONE]** Shipped the `.fresh()` builder capability (ratified in this review round) that unblocks the labs-nuxt add-session dead-end: scope builder `.fresh()` → distinct scope key (registry miss) → auth machine's `checking` state gains an eventless `always`-guarded transition (`isNewSession`) that skips `checkSession` entirely and lands in `idle`, identical to the no-session path. Files: `auth.types.ts`, `scope.types.ts`, `scope.utils.ts`, `scope.builder.ts`, `useAuth.ts`, `auth.machine.ts`, `auth.int.test.ts` (new AU-I13), labs-nuxt `useActorScopeSelector.ts` + `[...scopeSuffix].vue`. Verified: headless `pnpm type-check` green, unit 21/21, integration 44 pass / 1 fail (AU-I8 only, pre-existing, untouched) / 0 skip — net +1 passing test. Guest excluded from `.fresh()` at the type level (matches the one-guest ruling). Honest failure: manual browser confirmation of the actual login-form render, two-session list, and query-param survival across navigation is **not yet done** — needs a human click-through (steps logged in the fresh result).

- **Baseline check after all of the above:** tsc green; unit 21/21; integration 41 pass / 3 skip in the teardown/fixtures verification runs, separately 44 pass / 1 fail (AU-I8) / 0 skip after `.fresh()` added AU-I13 and unskipped AU-I9/I10. **AU-I6/AU-I7 are no longer red** (teardown fix supersedes the original baseline's "2 fail" for those two) — the sole remaining known red is **AU-I8**, a genuine unfixed product race, not a fixture or leak issue.

- **Not done this session:** `graphify update .` (CLAUDE.md rule) — flagged by multiple agents as deliberately skipped since only test/scoped source files changed; left for the orchestrating session to run once all edits for the day are final.

## 2026-07-03 — afternoon

- **[AU-I8 — FIXED]** Root-caused the register-flow race: `start("register")` resolved as soon as the machine merely entered the register subtree (prefix match) while still in `register.loading`, so a REGISTER submit sent then was silently dropped (only `register.available` handles it) → 30s timeout. Only AU-I8 tripped it — it runs cache-cold (query singleton, 5-min staleTime); AU-I9/AU-I10 ran warm and never hit the race. Fix: `start()` now settles on `${flow}.available` for all three flows. `packages/headless/src/modules/auth/useAuth.actions.client.ts`. The old skip-comments blaming a missing `clients_fields` fixture were stale — the fixture already existed on disk.

- **[AuthFlowTypes — DONE]** Added `AuthFlowTypes` enum (`LOGIN`/`RECOVER`/`REGISTER`) to `auth.types.ts`; `start(flow?: AuthFlowTypes)`. Swept string-literal callers: `auth.int.test.ts` (14 sites), labs-nuxt `useAuth` page (4), `client-vue` `Auth.vue` + `LoginPopover.vue` (4). Rebuilt headless dist so client-vue's type-check sees the new export.

- **[Manual multi-session smoke — 3 bugs found + fixed]** Operator ran a manual click-through in labs-nuxt (2 clients + 1 staff). Surfaced and fixed:
  - **C1 — selector pointer stomp:** `useActorScopeSelector.ts` `initFromRoute`/`watch(actorScope)` called `activate(scope, first(keys(sessions)))` on every load, overwriting a correctly restored/switched active pointer with the scope's first session (proven: a switch persisted to sessionStorage but reverted on reload). Fix: guard — skip activation when the store's active actor already matches the URL scope; only a genuine deep-link mismatch redirects the pointer.
  - **C2 — add-session dead-end on the same route:** clicking "Add another client" while already on `/useAuth/as/client` was a router no-op (identical fullPath, no remount). Fix: `?fresh=<Date.now() nonce>` query + page keyed by fullPath (`definePageMeta` key) so every add-session remounts and spawns a new `.fresh()` instance. `useActorScopeSelector.ts` + `app/pages/useAuth/[...scopeSuffix].vue`.
  - **C3 — fresh-key remount race:** a static `"fresh"` scope-key segment let a remounting page adopt the previous (already-authenticated) fresh instance just before its unmount destroyed it. Fix: unique key per call, `fresh:${++freshInstanceCount}`. `packages/headless/src/modules/scope/scope.utils.ts`.
  - New regression test: `session-store.reload.probe.int.test.ts` — proves an active-session switch persists across a reload (fresh module graph + `initStore` re-boot). This exonerated the store layer and pinned the reload-revert bug on the playground selector (C1).

- **[AccessRoleTypes.USER — DONE]** `packages/types/src/data/enums.ts`: added a proper `@deprecated` JSDoc on the `USER` alias (cart-1 backward compat, same `"user"` wire value as `STAFF`) pointing to `.STAFF`. Ruling stands: staff's wire value remains `"user"` — the enum member name is the canonical reference, the value is wire-compat only.

- **Baseline after the afternoon's fixes:** headless tsc green; unit 21/21; integration 46/46 (0 fail, 0 skip); client-vue type-check green (post-rebuild); labs-nuxt type-check green. Operator's manual smoke (2 clients + 1 staff): per-tab switch, refresh persistence, add-session from any page — all confirmed working. **Nothing committed** — tree stays uncommitted for review, per standing rule.

## 2026-07-04 — max-effort branch review + fixes

- **[review — DONE]** Full skill-run branch review: 10 finder angles → 1-vote verify → 21 candidates, 10 REFUTED with quoted evidence. 4 confirmed bugs fixed same session, each with a regression test (2 revert-proven):
  - **BUG A (severe)** — boot dead-token drop deleted the ACTIVE session's per-scope cookie whenever ANY session of that scope had a dead token (cookies are per-scope, held by the active session) → live user logged out to guest. Fix: drop the cookie only when the dead session's `actor_id` matches the cookie's. `session-store.store.ts` (`buildInitialState`). Test T1.
  - **BUG B (severe)** — a session auto-promoted by the write gate (e.g. logout of 1-of-2 clients) was never cookie-projected; the next write's reconcile dropped it, losing both sessions. Fix: `updateSession`'s gate now projects the resolved active token to its scope cookie when missing (`persistTokenToStorage sync:false`) — self-heals the "active session is always cookie-backed" invariant. `useSessionStore.actions.ts`. Test T2.
  - **BUG C** — `start()` waited only on `${flow}.available`; a register schema-load failure (lands sibling `register.unavailable`) blocked the full 60s timeout. Fix: settle on available OR unavailable. `useAuth.actions.client.ts`. Test AU-I14.
  - **BUG D** — the machine silently dropped a REGISTER received during `register.loading` (only `register.available` handled it; reachable via the shared `register()` action and `useInternals().send`). Fix: `register.loading` stashes the model + `pendingSubmit`; `register.available`'s always-guard replays it once ready; `loading.onError` clears the flag. `auth.machine.ts` (`AuthContext.pendingSubmit`). Test AU-I15.

- **[rulings — DONE]** Anti-stomp guard moved from the labs-nuxt selector into the store itself: `activate(actor)` with no id is now no-op if the scope's already active, else first-of-scope, else no-op (`useSessionStore.actions.ts`); selector simplified to a bare `activate(scope)` call. Fresh-instance registry accumulation under a missed `destroy()` ruled **PARKED** — contract-violation-only (documented `destroy()` contract + onLogout auto-removal cover the real consumer).

- **[cleanups — DONE]** Shared int-test helper module `packages/headless/src/__tests__/int-test-helpers.ts` (`clearSessionCookies` regex form + `makeFixtureOverrides`) replaces 5 drifted per-file copies — fixed a real `account.int.test.ts` bug (was clearing the nonexistent `upm_staff_session` instead of `upm_user_session`). Reload-probe test merged into `session-store.int.test.ts` (probe file deleted). 5 verifier-proven-orphan fixture JSONs deleted (2 others checked and kept — load-bearing via default MSW handlers). `start()` event now derived from the enum value (ternary removed); `useVerifyEmail` catch simplified; `dumpTokenFromStorage` no longer parses cookies on the `sync:false` path.

- **[bonus find — FIXED]** `session-store.int.test.ts` never stubbed `BroadcastChannel` — real native-channel broadcasts landed asynchronously on freshly re-imported modules' listeners, stomping just-activated sessions (test-isolation race). Fixed with a `FakeBroadcastChannel` stub + per-test reset.

- **Refutations (10, one line):** notable ones — reconcile keeping a stale active pointer when the cookie holds a different user is DESIGNED (per-tab divergence; wire auth always cookie-sourced); `getExpiresAt` null-flip unreachable (dead code path + null-guarded consumers); `add()` `initialised:true` safe (gated + session-transfer reloads).

- **Baseline after this run:** integration 50/50 twice consecutively, unit 21/21, tsc + client-vue + labs-nuxt typechecks green, `lint:fixtures` clean. **Nothing committed** — tree stays uncommitted for review.
