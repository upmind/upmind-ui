node_modules
dist
.nuxt
.output
.idea/
.vscode/
*.local
*.log
.DS_Store
*storybook.log
**/.firebase/**
.tsbuildinfo
bin/pnpm
bin/pnpx
tsconfig.tsbuildinfo
# .claude is tracked (agent config, rules + companions, commands); runtime/personal bits are not
.claude/.DS_Store
.claude/settings.local.json
.claude/tasks/
.claude/worktrees/


# Playwright
tests/Playwright/e2e/test-output/
tests/Playwright/e2e/reports/
test-results/
tests/Playwright/blob-report/
tests/Playwright/playwright/.cache/
tests/Playwright/e2e/support/secrets
tests/Playwright/e2e/snapshots/
# Canonical fixture pool (tests/fixtures/recordings/) IS committed — FE-2775.
# Only ad-hoc / brand-specific capture dirs (recordings-velia/, etc.) are ignored.
tests/fixtures/recordings-*/
.playwright-mcp/


# Git worktrees
.worktrees/

# Asset registry (post-build chunk manifests)
.asset-registry/

# Graphify outputs — regenerated by `pnpm graph` or the post-merge hook.
# Keep the tooling in .claude/scripts/graphify/ versioned; ignore generated artifacts.
graphify-out/
**/graphify-out/
repos/monorepo
repos/vue-app

# Corpus build reflection artifact — regenerated per build by docs/corpus/build.mjs
# and consumed by the drift gates via CI `needs:` (not a committed file, FE-2752 T3).
docs/corpus/.reflection.json

# gate:examples' throwaway type-check project — always removed before the gate
# exits; ignored defensively in case a run is interrupted (FE-2752 T8).
docs/corpus/.gate-examples-tmp-*/

# Transient agent-generated docs
docs/analysis/
docs/plans/
docs/audit/
docs/reviews/
docs/decisions/
docs/stories/
docs/sdd/
