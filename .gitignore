# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dist-ssr
coverage
*.local

# Environment files — NEVER commit real env files (they leak secrets into git
# history). Use committed *.example templates for shared, non-secret defaults.
.env
.env.*
!.env.example
!.env.*.example

/cypress/videos/
/cypress/screenshots/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
tsconfig.tsbuildinfo

# Graphify outputs (regenerated by the monorepo's `pnpm graph` workflow)
graphify-out/
