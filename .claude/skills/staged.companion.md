> Companion to the upmind-agent skill /staged (the staged route of `/start` — was `/code-wave`) — Upmind-monorepo-specific bindings/overrides.

Additive. These bind the generic "targeted subset" and "standing test-time ceiling" in base §E to this repo's concrete values, and name the governing decision record the base tells you to cite. Repo wins on any conflict.

## §E — finale verification pass bindings

- **Standing test-time ceiling = 30 minutes.** The staged run's finale verification pass is held inside a hard 30-minute ceiling. A pass that cannot fit is scoped down (fewer specs, targeted projects) — never waived, never run as a multi-hour suite.
- **Designated locale scope = EN.** The base's "repo's designated locale scope" is the EN locale here: scope the settled-branch gate to EN / targeted specs.
- **Governing testing decision record = ADR-021 (Testing Trophy, Agentic Workflow & Coverage Policy).** This is the "repo's testing decision record" base §E tells you to cite. It sets both the 30-minute ceiling and the EN / targeted-spec scope. Cite it; do not restate it.

## §A — prover contract-feeding backing

- The base's "code-author ≠ assertion-author" principle (§A, Builder ≠ prover; owned by `${CLAUDE_PLUGIN_ROOT}/rules/code-tests.md`) is codified for this repo by **ADR-021**. Cite ADR-021 alongside that rule when a prover work order needs a repo-governance backing.
