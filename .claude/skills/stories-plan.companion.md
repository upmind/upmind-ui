> Companion to the upmind-agent skill /stories-plan — Upmind-monorepo-specific bindings/overrides.

## Issue tracker

- The issue tracker is **Linear** (via the Linear MCP tools). Everywhere the base says "issue tracker" — including step 9 "Import to the Issue Tracker" and the `/stories-import` invocation — it means Linear.

## Actor set

- Organize parent stories by this repo's actor set: **client**, **staff**, **guest**. Create one user-facing parent story per actor the initiative actually touches (skip actors it does not). The base's `USER`/`ADMIN` examples are placeholders; use these actor keys instead.

## Import defaults

- Default `team` for the stories JSON: **FE**.
- Default `labels`: **["frontend"]**.
