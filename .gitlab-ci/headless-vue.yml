(headless-vue) test:
  extends: .pnpm
  stage: test
  needs:
    - dependencies
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event' && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG =~ /^headless-vue-v\d+(\.\d+)*$/
    - if: $CI_FORCE_HEADLESS_VUE == "true"
  script:
    - pnpm --filter headless-vue test

(headless-vue) check version:
  extends: .check version
  variables:
    ROOT_DIR: packages/headless-vue
  rules:
    - if: $CI_COMMIT_TAG =~ /^headless-vue-v\d+(\.\d+)*$/
    - if: $CI_FORCE_HEADLESS_VUE == "true"

(headless-vue) build:
  extends: .pnpm
  stage: build
  needs:
    - dependencies
    - (headless-vue) test
    - (headless) build
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event' && $CI_MERGE_REQUEST_TARGET_BRANCH_NAME == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG =~ /^headless-vue-v\d+(\.\d+)*$/
    - if: $CI_FORCE_HEADLESS_VUE == "true"
  artifacts:
    when: on_success
    access: all
    expire_in: "1 day"
    paths:
      - packages/headless-vue/dist/
  script:
    - pnpm --filter headless-vue build

(headless-vue) publish internal:
  extends: .pnpm
  stage: publish internal
  rules:
    - if: $CI_COMMIT_TAG =~ /^headless-vue-v\d+(\.\d+)*$/
  needs:
    - dependencies
    - (headless-vue) build
    - (headless-vue) check version
  dependencies:
    - dependencies
    - (headless-vue) build
  script:
    - echo "${INTERNAL_NPMRC}" > .npmrc
    - pnpm --filter headless-vue publish

(headless-vue) publish to npm:
  extends: .pnpm
  stage: publish external
  rules:
    - if: $CI_COMMIT_TAG =~ /^headless-vue-v\d+(\.\d+)*$/
      when: manual
  needs:
    - dependencies
    - (headless-vue) build
    - (headless-vue) publish internal
  dependencies:
    - dependencies
    - (headless-vue) build
  script:
    - pnpm --filter headless-vue publish --access public ${NPM_PUBLISH_NPM_EXTRA_ARGS}
