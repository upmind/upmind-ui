{
  "extends": "@vue/tsconfig/tsconfig.dom.json", // Extend Vue's base config directly
  "compilerOptions": {
    "composite": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    },
    "outDir": "dist/app",
    "target": "esnext", // Or your target environment (e.g., "es5", "es2020")
    "lib": ["esnext", "dom"] // Or your required browser libraries
  },
  "//": "No other changes needed here"
}
