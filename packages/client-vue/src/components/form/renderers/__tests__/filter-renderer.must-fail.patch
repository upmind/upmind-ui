Negative control for filter-renderer.test.ts.

Scopes the Verified element at the COLUMN rather than at its operator leaf — the
declaration the bespoke `Filter` uiType used to make, and the one this change
removed. Any assertion that reads what a Control element scopes goes red; one
that only counts elements, or checks each carries an i18n key, stays green.

diff --git a/packages/headless/src/modules/client-email/client-email.schemas.ts b/packages/headless/src/modules/client-email/client-email.schemas.ts
--- a/packages/headless/src/modules/client-email/client-email.schemas.ts
+++ b/packages/headless/src/modules/client-email/client-email.schemas.ts
@@ -255,7 +255,7 @@
       },
       {
         type: "Control",
-        scope: "#/properties/filters/properties/verified/properties/eq",
+        scope: "#/properties/filters/properties/verified",
         i18n: "form.verified_filter",
         options: { format: "button-group" }
       },
