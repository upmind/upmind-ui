Negative control for the INLINE layout the tri-state is owed (P1-R3).

Drops the field's inline override, so a boolean control falls back under its
label instead of beside it — the exact rejected treatment. Any assertion that
reads the field wrapper's resolved direction goes red; one that only asserts a
control rendered stays green.

Apply from the repo root: `git apply <this file>`; revert with `git apply -R`.

diff --git a/packages/client-vue/src/components/form/renderers/FilterRenderer.vue b/packages/client-vue/src/components/form/renderers/FilterRenderer.vue
--- a/packages/client-vue/src/components/form/renderers/FilterRenderer.vue
+++ b/packages/client-vue/src/components/form/renderers/FilterRenderer.vue
@@ -333,7 +333,6 @@
     noLabel: isToggleGroup.value || !!merged.noLabel,
     uiConfig: {
       form: {
-        field: "flex-row flex-wrap items-center gap-3",
         label: "w-auto shrink-0",
         control: "w-auto"
       }
