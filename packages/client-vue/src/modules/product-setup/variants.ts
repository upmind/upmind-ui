import { cva } from "class-variance-authority";

export const formRootVariants = cva("flex w-full flex-col gap-6");
// full-width error alert + Continue submit
export const formFullVariants = cva("w-full");
