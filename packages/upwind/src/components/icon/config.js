import { upwConfig } from "../../utils";

// -----------------------------------------------------------------------------
export default {
  root: upwConfig(
    "flex flex-shrink-0 fill-current stroke-current text-current"
  ),
};
