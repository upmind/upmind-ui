Negative control for the reshaped `createTraceabilityCheck` (`malformedStepDefs`).

Returns an empty malformed list regardless of what the matcher found. The old
shape carried the malformed case inside the aggregate `ok` flag; the reshape
retires `ok`, so `malformedStepDefs` is now the ONLY channel a pattern that
fails to compile as a cucumber expression travels on. Emptied, an unregistered
custom parameter type is silently a step nothing can ever match.

The assertion that must go RED: the one asserting `malformedStepDefs` names a
catalog pattern that does not compile.

Apply from the repo root: `git apply <this file>`; revert with `git apply -R`.

Proven RED 2026-08-12, blind
(`pnpm --filter @upmind-automation/scenario-harness exec vitest run src/steps/__tests__/traceability.test.ts`):
  x FAILS a pattern that does not compile, surfacing it as data rather than throwing
      -> expected [] to strictly equal [ 'a value of {unregisteredCustomParameterType}' ]
  1 failed | 12 passed. Reverted with `git apply -R`; suite back to 13 passed.

Behaviour shift measured by the author (NOT the red proof) — with a catalog
carrying `"a {nope}"`:
  before: malformedStepDefs.length = 1
  after:  malformedStepDefs.length = 0

diff --git a/packages/scenario-harness/src/steps/traceability.ts b/packages/scenario-harness/src/steps/traceability.ts
--- a/packages/scenario-harness/src/steps/traceability.ts
+++ b/packages/scenario-harness/src/steps/traceability.ts
@@ -358,7 +358,7 @@ export function createTraceabilityCheck(
         matcher.compiledIndexes.has(index) && !matchedStepDefs.has(index)
     ),
     duplicatedPatterns: findDuplicatedPatterns(catalog, catalogs),
-    malformedStepDefs: matcher.malformedStepDefs
+    malformedStepDefs: []
   };
 }
 
