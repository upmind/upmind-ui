--- a/packages/headless/src/modules/query/useQueryCriteria.ts
+++ b/packages/headless/src/modules/query/useQueryCriteria.ts
@@ -71,7 +71,7 @@ function toWireFilterValue(operator: string, value: unknown): string {
       operator
     )
   )
-    return `%${value}%`;
+    return `${value}`;
   if (isBoolean(value)) return value ? "1" : "0";
   if (isArray(value)) return join(value, ",");
   return String(value);
