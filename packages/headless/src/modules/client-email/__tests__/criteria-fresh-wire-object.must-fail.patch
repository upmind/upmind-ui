diff --git a/packages/headless/src/modules/query/useQueryCriteria.ts b/packages/headless/src/modules/query/useQueryCriteria.ts
index 6b40b4e24..81c4e6910 100644
--- a/packages/headless/src/modules/query/useQueryCriteria.ts
+++ b/packages/headless/src/modules/query/useQueryCriteria.ts
@@ -91,6 +91,8 @@ function toWireFilterValue(operator: string, value: unknown): string {
  * through. A FRESH object every call so `useQuery`'s `isEqual` guard is a value
  * comparison.
  */
+const wireFilters: RequestFilters = {};
+
 export function translateQuery(
   schema: JsonSchema,
   model: Record<string, unknown>
@@ -109,7 +111,7 @@ export function translateQuery(
         },
         result
       ),
-    {}
+    wireFilters
   );
 
   const sortFields = get(
