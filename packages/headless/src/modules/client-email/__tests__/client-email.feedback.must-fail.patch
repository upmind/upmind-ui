diff --git a/packages/headless/src/modules/client-email/client-email.services.ts b/packages/headless/src/modules/client-email/client-email.services.ts
--- a/packages/headless/src/modules/client-email/client-email.services.ts
+++ b/packages/headless/src/modules/client-email/client-email.services.ts
@@ -1,5 +1,6 @@
 /** @internal */
 import { computed, ref } from "vue";
+import { useFeedback } from "../feedback";
 import { useQuery, invalidateQueryByKey } from "../query";
 import { resolveClientId, useActiveSession } from "../session-store";
 import { useI18n } from "../system-localisation";
@@ -386,7 +387,7 @@ export const createClientEmailServices = (
   const clientId = addressableClientId(scopeContext);
 
   const captureError: ClientEmailErrorCapture = error => {
-    mutationError.value = mapToHeadlessError(error);
+    useFeedback().addError(mapToHeadlessError(error).message);
   };
 
   return {
