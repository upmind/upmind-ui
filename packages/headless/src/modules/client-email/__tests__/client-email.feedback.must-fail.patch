--- a/packages/headless/src/modules/client-email/client-email.services.ts
+++ b/packages/headless/src/modules/client-email/client-email.services.ts
@@ -1,5 +1,6 @@
 /** @internal */
 import { computed, ref } from "vue";
+import { useFeedback } from "../feedback";
 import { useQuery, invalidateQueryByKey } from "../query";
 import { useActiveSession } from "../session-store";
 import { useI18n } from "../system-localisation";
@@ -403,7 +404,8 @@
   const clientId = resolveClientId(scopeContext);
 
   const captureError: ClientEmailErrorCapture = error => {
-    mutationError.value = mapToHeadlessError(error);
+    const mapped = mapToHeadlessError(error);
+    useFeedback().addError({ title: mapped.message, data: mapped.data });
   };
 
   return {
