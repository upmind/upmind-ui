Negative control for the module's TWO-composable public surface (AC-24).

Amputates the manager from the barrel, leaving the collection exported. The
module still builds, the collection still boots, and every consumer that only
lists emails stays green — which is exactly the silent shape the control exists
for: the per-email editor a row hands off to simply stops being reachable from
`@upmind-automation/headless`.

Refreshed 2026-08-11: the Phase 2 barrel dropped the "NO SCENARIO KEYS HERE"
note that the previous revision of this patch carried as trailing context, so
that revision no longer applied.

Apply from the repo root: `git apply <this file>`; revert with `git apply -R`.

Reds (headless unit lane, measured 2026-08-11 — 2 failed, 6 passed):
  client-email.surface.test.ts  "AC-24 offers BOTH composables — the collection
                                  and the per-email editor"
                                "AC-24 exports exactly the curated value
                                  surface — nothing internal leaks"

`published-exports.test.ts` stays GREEN, which is the point: it measures the
package barrel's own contract, not this module's, so the two controls are
independent and neither stands in for the other.

diff --git a/packages/headless/src/modules/client-email/index.ts b/packages/headless/src/modules/client-email/index.ts
index 58661a631..595a8d7c6 100644
--- a/packages/headless/src/modules/client-email/index.ts
+++ b/packages/headless/src/modules/client-email/index.ts
@@ -18,10 +18,6 @@

 // --- Composables (collection + manager)
 export { useClientEmails, type UseClientEmails } from "./useClientEmails";
-export {
-  useClientEmailManager,
-  type UseClientEmailManager
-} from "./useClientEmailManager";

 // --- Scope matrices — one per composable, both public
 export {
