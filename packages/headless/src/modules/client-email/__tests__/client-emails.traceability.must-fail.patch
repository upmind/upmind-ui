Negative control for client-emails.traceability.test.ts (Task 21).

Withdraws the step definition implementing the feature's sort step. The feature
still writes that step, so the pair has drifted: the check must name it in
`unmatchedFeatureSteps`, and `sortBy` must show as declared-covered but fired by
nothing.

Apply from the repo root: `git apply <this file>`; revert with `git apply -R`.

Proven RED 2026-08-08
(`pnpm --filter @upmind-automation/headless exec vitest run --project unit client-emails.traceability`):
  x matches every feature step to a step definition, and back
      -> expected [ Array(1) ] to deeply equal []
  x fires every action it declares as covered
      -> declared covered but fired by no step: sortBy
  2 failed

diff --git a/packages/headless/src/modules/client-email/__tests__/client-emails.steps.ts b/packages/headless/src/modules/client-email/__tests__/client-emails.steps.ts
index bcbf35bb4..98e305e74 100644
--- a/packages/headless/src/modules/client-email/__tests__/client-emails.steps.ts
+++ b/packages/headless/src/modules/client-email/__tests__/client-emails.steps.ts
@@ -149,12 +149,6 @@ export const clientEmailsSteps = defineSteps(({ Given, When, Then }) => {
     })
   );
 
-  When("the client sorts the collection by address descending", world =>
-    world.fire(CLIENT_EMAILS_COVERED_ACTIONS.sortBy, [
-      { field: "email", dir: "desc" }
-    ])
-  );
-
   Then("the collection holds {int} addresses", (world, total) =>
     settles(() => world.expectContext({ pagination: { total } }))
   );
