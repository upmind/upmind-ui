Negative control — the READ path's SCOPE SEAM (AC-2), the story's headline claim.

Hardwires the SESSION's own client into the two READ urls — the collection's
list request (`loadList`) and the manager's per-address read (`loadOne`) — by
re-reading `useActiveSession().useContext().activeUser` inside each function
instead of addressing the `clientId` ref the scope resolved.

The seam is left standing. `resolveClientId` still exists, still branches on
`.for('client', id)`, still feeds `queryKey`, `isAddressable`, `enabled`, and
every mutation. Only the two read urls stop consulting it. That is the FE-2824
shape stated exactly: the structure is all present, the capability is gone.

This is the pre-conversion defect restored verbatim. On `main` the module's
`loadList` read `const { clientId } = useSession()` in its own body
(`modules/client/address/services.ts:33-34`) and every other request function
did the same — `parity.yaml` W12/L10, the module's live FE-2824. A request that
addresses the session's client instead of the scope's is precisely the failure
AC-2 exists to detect.

Declares AC-2 — "the collection addresses the scope's client, not the session's
by accident": the captured request url's `{clientId}` segment must equal the
SCOPE-resolved client id.

Should flip RED — any AC-2 read-back in which the scope-resolved client and the
session's `activeUser` are DIFFERENT ids.

REBASE NOTE (this revision): the prior cut of this patch carried a context
block against a `loadList(params: Partial<QueryParams<...>> = { pagination: {
limit: 0 } }, clientId)` signature that no longer exists — `loadList` moved to
a zero-arg-default, single-parameter `loadList(clientId)` (the query schema's
own `pagination.limit` default now supplies the `limit: 0`, per the criteria
seam this story adds), and `git apply --check` failed against current
`client-address.services.ts` as a result. This revision re-cuts BOTH hunks
against the current zero-arg `loadList` and the unchanged `loadOne`. The
MUTATION ITSELF is unchanged — same two lines swapped from `clientId.value` to
`activeUser.value?.id`, same seam left standing, same AC-2 declared. Verified
`git apply --check` clean against
`packages/headless/src/modules/client-address/client-address.services.ts` at
`b8549ef82923cf8ffaa8713bd5076598b66082e9`, and the mutated file compiles clean
under `vue-tsc -p tsconfig.build.json --noEmit` (the two errors that survive —
`brand.services.ts:111` and `client-company.schemas.ts:82` — are pre-existing
and outside this file; see `client-address.brand-config.int.test.ts`'s
pre-existing 3 failures, root-caused to `brand.services.ts:112-114`).

MEASURED (prior revision), and stated plainly: against the suite at
`67f8be413` this mutant flips NOTHING — 112 passed / 1 failed, identical to the
unmutated baseline (the one failure is AC-20 / finding F6, escalated per
`review-notes.md` §7.6, red with or without it). That is the finding, not a
defect in the mutant. With a single live actor whose session client IS the
scope-resolved client, "read the session" and "read the seam" resolve to the
same id, so a url assertion passes either way. Re-measurement against the
re-cut patch is the prover's, applied blind, per `agent-seat-separation.md` and
its companion's must-fail-patch seam (developer authors the mutant, prover
verifies it red and reverts — never reading src to construct it).

It is also why the EARLIER AC-2 control — which deleted the `.for('client', id)`
branch out of `resolveClientId` altogether — flipped nothing and had to be
withdrawn; see `client-address.scope-identity.must-fail.patch`, which attacks
the PIN instead and therefore declares AC-30, not AC-2.

The discriminator AC-2 needs is a setup in which the scope-resolved client and
the session's `activeUser` are different ids. `client-address.scope-identity.
int.test.ts:154` ("AC-2 reads the SCOPE-CONTEXT client's list while the
session's own activeUser is a different client") is exactly that setup, and
carries it independently of this file.

MEASURED (prover, applied blind, per agent-seat-separation.md's must-fail-
patch seam — the developer authors the mutant, the prover verifies it red and
reverts): `git apply --check` clean against the current tree. Baseline (this
module's full suite, unmutated): 3 failed | 150 passed (153) — the 3
pre-existing `client-address.brand-config.int.test.ts` AC-20/AC-21 failures,
root-caused to `brand.services.ts:112-114`, unrelated. Applied: 4 failed | 149
passed (153) — exactly the one AC-2 test named above flips (its `useUrl`
assertion resolves against `OTHER_CLIENT_ID`, which the mutation no longer
addresses), nothing else moves. This supersedes the "not yet demonstrated
red" line an earlier revision left here — that was true only in the absence
of the two-different-client setup, which now exists. Reverted after
measurement (`git apply -R`), 2026-08-23 (re-measured; the module total moved
152 -> 153 with the new AC-41 `hasError` proof).

What this deliberately does NOT touch:
  - The addressability guard. `isAddressable(clientId.value)` keeps reading the
    seam in both functions, so AC-3 / AC-11 / AC-13's empty-capture-log
    read-backs stay green and a red here can never be mistaken for an auth
    regression.
  - `queryKey`. Still scope-keyed, so a red localises to the URL — the thing
    AC-2 asserts — and not to cache partitioning.
  - The WRITE path. `add` / `update` / `remove` / `setDefault` still address the
    seam. The mutation is read-only by construction, which is what makes it
    AC-2's control rather than a second AC-30 control.

Compiles clean under the mutation (`pnpm --filter @upmind-automation/headless
type-check`, exit 0 for this file — see REBASE NOTE) — a control that cannot
build proves nothing.

diff --git a/packages/headless/src/modules/client-address/client-address.services.ts b/packages/headless/src/modules/client-address/client-address.services.ts
index 385d7627a..000000000 100644
--- a/packages/headless/src/modules/client-address/client-address.services.ts
+++ b/packages/headless/src/modules/client-address/client-address.services.ts
@@ -190,8 +190,9 @@ function loadList(
   clientId: ClientAddressServices["clientId"]
 ): ClientAddressListQuery {
   const { list, useUrl } = useQuery();
+  const { activeUser } = useActiveSession().useContext();
   const targetUrl = () =>
-    useUrl(`clients/${clientId.value}/addresses`, {
+    useUrl(`clients/${activeUser.value?.id}/addresses`, {
       with: ["region", "country"].join()
     });
   const url = targetUrl();
@@ -253,6 +254,7 @@ async function loadOne(
   if (!id) return undefined;
 
   const { get: getOne, useUrl } = useQuery();
+  const { activeUser } = useActiveSession().useContext();
 
   if (!isAddressable(clientId.value)) {
     return Promise.reject(new NotAuthenticatedError());
@@ -260,7 +262,7 @@ async function loadOne(
 
   return getOne<IAddress, Address>({
     queryKey: [...queryKey, { client: clientId.value }, id],
-    url: useUrl(`clients/${clientId.value}/addresses/${id}`, {
+    url: useUrl(`clients/${activeUser.value?.id}/addresses/${id}`, {
       with: ["region", "country"].join()
     }),
     select: mapAddress,
