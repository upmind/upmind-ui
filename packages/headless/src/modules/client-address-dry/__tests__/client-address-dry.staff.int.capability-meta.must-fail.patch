--- a/packages/headless/src/modules/client-address-dry/useClientAddressesDry.meta.staff.ts
+++ b/packages/headless/src/modules/client-address-dry/useClientAddressesDry.meta.staff.ts
@@ -40,7 +40,10 @@
      */
     canList: capabilities.canList,
     canCreate: capabilities.canCreate,
     canUpdate: capabilities.canUpdate,
-    canDelete: capabilities.canDelete
+    // MUTANT (must-fail: capability-meta): hardcoded regardless of the
+    // capabilities bundle — a revoked `delete_client_address` staff session
+    // would still read `canDelete: true` from `useMeta()` (AC-B5).
+    canDelete: true
   };
 }
