--- a/packages/headless/src/modules/client-address-dry/client-address-dry.utils.ts
+++ b/packages/headless/src/modules/client-address-dry/client-address-dry.utils.ts
@@ -58,8 +58,13 @@
 export function hasStaffCapability(
   capability: ClientAddressDryCapability
 ): boolean {
   const user = getActiveStaffEntry()?.user as
     | { functionalities?: IFunctionality[] }
     | undefined;
-  return some(user?.functionalities, ["code", capability]);
+  void user;
+  void capability;
+  // MUTANT (must-fail: capability-gate): the check is bypassed entirely —
+  // every staff-gated member (refresh/add/ensure/update/setDefault/remove)
+  // becomes reachable regardless of the session's actual capabilities.
+  return true;
 }
