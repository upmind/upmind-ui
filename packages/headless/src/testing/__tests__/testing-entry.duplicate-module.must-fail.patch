Negative control for the published test entry's duplicate-module law
(`packages/headless/src/testing/index.ts`, `byModule`).

Withdraws the duplicate-key guard, leaving the silent last-wins the design
exists to refuse: a module whose `__tests__/` holds two `.feature` files (or two
`.steps.ts` files) would publish one and drop the other, with nothing to read
the loss off. The spec's duplicate-module claim must therefore go RED — with the
guard gone, collecting a colliding pair returns a map instead of throwing.

Apply from the repo root: `git apply <this file>`; revert with `git apply -R`
(never `git checkout --`).

Proven RED: pending — the prover applies this blind, records the command, the
failing assertion and its message here, then reverts.

diff --git a/packages/headless/src/testing/index.ts b/packages/headless/src/testing/index.ts
index 0ebe242..280df34 100644
--- a/packages/headless/src/testing/index.ts
+++ b/packages/headless/src/testing/index.ts
@@ -15,7 +15,7 @@
  * the vitest lanes and is inert in a process that is neither.
  */
 
-import { has, reduce, set } from "lodash-es";
+import { reduce, set } from "lodash-es";
 import type { StepCatalog } from "@upmind-automation/scenario-harness";
 
 // -----------------------------------------------------------------------------
@@ -34,11 +34,6 @@ const byModule = <T>(globbed: Record<string, T>): Record<string, T> =>
     (collected, artefact, path) => {
       const moduleName = MODULE.exec(path)?.[1] as string;
 
-      if (has(collected, [moduleName]))
-        throw new Error(
-          `@upmind-automation/headless/testing: "${moduleName}" holds more than one artefact of the same kind — "${path}" collides with one already collected. One .feature and one .steps.ts per module.`
-        );
-
       return set(collected, [moduleName], artefact);
     },
     {} as Record<string, T>
