Negative control for the armed track's scope (AC3.4 · design §7.5 · ESC5).

Drops the scope from the armed track's header. The track still reads as armed —
its name badge still recolours — so nothing looks broken; what disappears is the
one line that tells the reader a staff track is DECLARED at a staff scope while
the acting-for picker greys the staff actor row. That is the mitigation the
design owes for the matrix/feature disagreement, and without it the pane hands
the reader a bug report instead of an open question.

Apply from the repo root: `git apply <this file>`; revert with `git apply -R`.

Reds: scenario-pane.spec.ts — the armed-track case (the armed track names the
scope it runs at).

FOR THE PROVER: authored by the developer seat from the source line it mutates,
apply-checked with `git apply --check` and reverted; it has NOT been run against
a spec, because the spec is yours and did not exist when this was written. Apply
it blind, confirm the red, then revert.

diff --git a/playgrounds/labs-nuxt/app/components/sheets/ScenarioPane.vue b/playgrounds/labs-nuxt/app/components/sheets/ScenarioPane.vue
--- a/playgrounds/labs-nuxt/app/components/sheets/ScenarioPane.vue
+++ b/playgrounds/labs-nuxt/app/components/sheets/ScenarioPane.vue
@@ -44,17 +44,6 @@
               variant="muted"
               size="sm"
             />
-            <Badge
-              v-if="isArmed(track) && trackScopePath"
-              :label="t('labs.scenario_track_scope', { scope: trackScopePath })"
-              color="neutral"
-              variant="minimal"
-              size="sm"
-              :data-attrs="{
-                'data-test-key': 'scenario-track-scope',
-                'data-test-value': trackScopePath
-              }"
-            />
           </header>

           <ol :class="styles.scenarioPane.steps">
