Negative control for the unreached-playlist state (AC3.4 · ESC6 · S23).

Removes the empty state: with no playlist the Feature section renders its
heading over an empty list and says nothing at all. This is the state the pane
is in TONIGHT — `corpus.source.ts`'s `featureText` is `""` until `ESC6` is ruled
— so the silent version is a pane that looks broken on every load rather than a
pane that reports a known, ruled gap.

Apply from the repo root: `git apply <this file>`; revert with `git apply -R`.

Reds: scenario-pane.spec.ts — the no-feature-text case (with no playlist the
pane renders cleanly and says so, through a `labs.*` key).

FOR THE PROVER: authored by the developer seat from the source line it mutates,
apply-checked with `git apply --check` and reverted; it has NOT been run against
a spec, because the spec is yours and did not exist when this was written. Apply
it blind, confirm the red, then revert.

diff --git a/playgrounds/labs-nuxt/app/components/sheets/ScenarioPane.vue b/playgrounds/labs-nuxt/app/components/sheets/ScenarioPane.vue
--- a/playgrounds/labs-nuxt/app/components/sheets/ScenarioPane.vue
+++ b/playgrounds/labs-nuxt/app/components/sheets/ScenarioPane.vue
@@ -21,15 +21,7 @@
         {{ t("labs.scenario_feature") }}
       </h2>

-      <p
-        v-if="isEmpty(tracks)"
-        :class="styles.scenarioPane.empty"
-        data-test-key="scenario-feature-empty"
-      >
-        {{ t("labs.scenario_feature_pending") }}
-      </p>
-
-      <div v-else :class="styles.scenarioPane.playlist">
+      <div :class="styles.scenarioPane.playlist">
         <article
           v-for="track in tracks"
           :key="track.line"
