diff --git a/playgrounds/labs-nuxt/app/components/factory/surfaces/ListSurface.vue b/playgrounds/labs-nuxt/app/components/factory/surfaces/ListSurface.vue
index be7f4977b..68cf4143c 100644
--- a/playgrounds/labs-nuxt/app/components/factory/surfaces/ListSurface.vue
+++ b/playgrounds/labs-nuxt/app/components/factory/surfaces/ListSurface.vue
@@ -138,7 +138,7 @@
  */

 import { getCoreRowModel, useVueTable } from "@tanstack/vue-table";
-import { computed } from "vue";
+import { computed, ref } from "vue";
 import {
   Alert,
   Icon,
@@ -231,17 +231,12 @@ const columnFiltersState = computed<ColumnFiltersState>(() =>
   map(entries(tableModel.value.filter), ([id, value]) => ({ id, value }))
 );

+const localSorting = ref<SortingState>([]);
+
 const onSortingChange: OnChangeFn<SortingState> = updaterOrValue => {
-  const next = isFunction(updaterOrValue)
-    ? updaterOrValue(sortingState.value)
+  localSorting.value = isFunction(updaterOrValue)
+    ? updaterOrValue(localSorting.value)
     : updaterOrValue;
-  props.table?.emit({
-    type: "sort",
-    sort: map(next, entry => ({
-      field: entry.id,
-      dir: entry.desc ? "desc" : "asc"
-    }))
-  });
 };

 const onColumnFiltersChange: OnChangeFn<
@@ -273,7 +268,7 @@ const vueTable = useVueTable({
   manualPagination: true,
   state: {
     get sorting() {
-      return sortingState.value;
+      return localSorting.value;
     },
     get columnFilters() {
       return columnFiltersState.value;
