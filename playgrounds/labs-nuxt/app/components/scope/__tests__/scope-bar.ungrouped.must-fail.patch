Negative control for the ONE grouped cluster (T2.1 · G11 · AC1.1).

Takes the grouping role off the bar. The three segments still render, still in
the same order, still separated by the same rules — only the element that made
them ONE control is gone, so the cluster degrades into three loose neighbours
that happen to sit next to each other. The accessible name stays on the element
(the `labs.*` key is not what is under test here), which is exactly the failure
worth pinning: a labelled `div` still reads fine in a snapshot and still passes
any assertion that only counts segments.

Apply from the repo root: `git apply <this file>`; revert with `git apply -R`.

Reds: scope-bar.spec.ts — the case that resolves the bar BY ITS GROUP ROLE and
finds all three segments inside that one group.

  PROVER VERIFIED (blind) — 2026-08-12, component lane:
  `vitest run --project component app/components/scope/__tests__/scope-bar.spec.ts`.
  Applied from the repo root, RED, reverted with `git apply -R`, tree restored.

  RED set: 6/6 — the declared grouping case fails on its own assertion
  (`[role='group']` count 0), and every other case reds behind it because the
  spec resolves the bar BY that group role. Blunt but honest: with no group
  there is no cluster to make any statement about.

diff --git a/playgrounds/labs-nuxt/app/components/scope/ScopeBar.vue b/playgrounds/labs-nuxt/app/components/scope/ScopeBar.vue
--- a/playgrounds/labs-nuxt/app/components/scope/ScopeBar.vue
+++ b/playgrounds/labs-nuxt/app/components/scope/ScopeBar.vue
@@ -1,7 +1,6 @@
 <template>
   <div
-    role="group"
     :aria-label="t('labs.scope_bar')"
     :class="styles.scopeBar.root"
     data-test-key="scope-bar"
   >
