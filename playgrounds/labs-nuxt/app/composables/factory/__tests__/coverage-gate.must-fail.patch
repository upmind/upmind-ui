Negative control for coverage-gate.int.spec.ts (Task 22 falsifiability).

Withdraws `sortBy` from the step catalog's declared covered set. The live cell
still publishes `sortBy` and its `@scenario-include` tag is untouched, so the
gate must red it with cause `uncovered` — a tag never substitutes for a
scenario.

Apply from the repo root: `git apply <this file>`; revert with `git apply -R`.

Proven RED 2026-08-08 (`pnpm --filter @upmind-automation/labs-nuxt test -t "@AC8"`):
  x returns no red verdict for any live action
      -> expected [ 'sortBy: uncovered' ] to deeply equal []
  x requires the two @AC7 scenarios for the query write paths
      -> sortBy: expected 'red' to be 'covered'
  x goes red on a step naming an action the cell no longer publishes
      -> expected [ 'sortBy', 'retire' ] to deeply equal [ 'retire' ]
  3 failed | 3 passed

diff --git a/packages/headless/src/modules/client-email/__tests__/client-emails.steps.ts b/packages/headless/src/modules/client-email/__tests__/client-emails.steps.ts
index bcbf35bb4..32333e5fa 100644
--- a/packages/headless/src/modules/client-email/__tests__/client-emails.steps.ts
+++ b/packages/headless/src/modules/client-email/__tests__/client-emails.steps.ts
@@ -32,7 +32,6 @@ export const CLIENT_EMAILS_COVERED_ACTIONS = {
   refresh: "refresh",
   remove: "remove",
   setDefault: "setDefault",
-  sortBy: "sortBy",
   verify: "verify"
 } as const;
 
