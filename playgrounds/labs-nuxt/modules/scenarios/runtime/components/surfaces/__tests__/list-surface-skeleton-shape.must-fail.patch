Negative control for the skeleton's declared SHAPE (C8).

Collapses the skeleton row to one column-spanning cell, so the placeholder no
longer stands in for the declared row: the operator waits on a shape that is
not the shape that arrives, which is the generic block the finding rejected.

Apply from the repo root: `git apply <this file>`; revert with `git apply -R`.

Reds: the loading-state specs — one skeleton cell per declared column.

diff --git a/playgrounds/labs-nuxt/modules/scenarios/runtime/components/surfaces/ListSurface.vue b/playgrounds/labs-nuxt/modules/scenarios/runtime/components/surfaces/ListSurface.vue
--- a/playgrounds/labs-nuxt/modules/scenarios/runtime/components/surfaces/ListSurface.vue
+++ b/playgrounds/labs-nuxt/modules/scenarios/runtime/components/surfaces/ListSurface.vue
@@ -150,7 +150,7 @@
       <TableBody>
         <template v-if="meta.isLoading">
           <TableRow v-for="placeholder in SKELETON_ROWS" :key="placeholder">
-            <TableCell v-for="element in columnElements" :key="element.scope">
+            <TableCell :colspan="columnCount">
               <Skeleton :class="styles.listSurface.skeletonCell" />
             </TableCell>
             <TableCell
