diff --git a/playgrounds/labs-nuxt/modules/scenarios/runtime/components/ModuleRenderer.vue b/playgrounds/labs-nuxt/modules/scenarios/runtime/components/ModuleRenderer.vue
index 8a649861b..e62a9acfc 100644
--- a/playgrounds/labs-nuxt/modules/scenarios/runtime/components/ModuleRenderer.vue
+++ b/playgrounds/labs-nuxt/modules/scenarios/runtime/components/ModuleRenderer.vue
@@ -1,21 +1,21 @@
 <template>
   <FormFlowSurface
-    v-if="descriptor.archetype.archetype === ARCHETYPE.FORM_FLOW"
+    v-if="liveArchetype === ARCHETYPE.FORM_FLOW"
     :snapshot="descriptor.snapshot"
     :actions="port.actions"
   />
   <DetailSurface
-    v-else-if="descriptor.archetype.archetype === ARCHETYPE.DETAIL"
+    v-else-if="liveArchetype === ARCHETYPE.DETAIL"
     :snapshot="descriptor.snapshot"
     :actions="port.actions"
   />
   <ActionPanelSurface
-    v-else-if="descriptor.archetype.archetype === ARCHETYPE.ACTION_PANEL"
+    v-else-if="liveArchetype === ARCHETYPE.ACTION_PANEL"
     :snapshot="descriptor.snapshot"
     :actions="port.actions"
   />
   <ListSurface
-    v-else-if="descriptor.archetype.archetype === ARCHETYPE.LIST"
+    v-else-if="liveArchetype === ARCHETYPE.LIST"
     :snapshot="descriptor.snapshot"
     :actions="port.actions"
     :table="port.table"
@@ -33,7 +33,8 @@
  * itself; each surface owns its own state/content rendering.
  */
 
-import { ARCHETYPE } from "@upmind-automation/scenario-harness";
+import { computed } from "vue";
+import { ARCHETYPE, classify } from "@upmind-automation/scenario-harness";
 import ActionPanelSurface from "./surfaces/ActionPanelSurface.vue";
 import DetailSurface from "./surfaces/DetailSurface.vue";
 import FormFlowSurface from "./surfaces/FormFlowSurface.vue";
@@ -42,4 +43,8 @@ import type { ModuleRendererProps } from "./ModuleRenderer.types";
 // -----------------------------------------------------------------------------
 
 const { descriptor, port, presentation } = defineProps<ModuleRendererProps>();
+
+const liveArchetype = computed(
+  () => classify(descriptor.snapshot, !!port.table).archetype
+);
 </script>
