diff --git a/playgrounds/labs-nuxt/modules/scenarios/runtime/composables/useTableChannel.ts b/playgrounds/labs-nuxt/modules/scenarios/runtime/composables/useTableChannel.ts
index 071be12c9..348d55067 100644
--- a/playgrounds/labs-nuxt/modules/scenarios/runtime/composables/useTableChannel.ts
+++ b/playgrounds/labs-nuxt/modules/scenarios/runtime/composables/useTableChannel.ts
@@ -124,7 +124,7 @@ export function useTableChannel(cell: TableChannelCell): DeclaringTableChannel {
       // paginate — the composable exposes only ±1 steppers; a jump-to-page and a
       // live page-size are named gaps in `useQuery`, filed.
       const current = context.pagination.value.page ?? 1;
-      if (intent.page > current) actions.nextPage();
+      if (intent.page > current) actions.prevPage();
       else if (intent.page < current) actions.prevPage();
     }
   };
