Negative control for `ESC6` at the declaration (T4.4).

Swaps the `corpus.source` seam import for the banned
`@upmind-automation/headless/testing/*` specifier — the shape a seat reaches
for the moment the seam looks like indirection for its own sake. The
declaration is in none of eslint 8h's four test-lane globs, so 8g errors and
`lint` (a gate T2.5 and T6.2 both run) goes red; the module-lane assertion that
the declaration's `tracks.feature`/`tracks.catalog` ARE the seam's own exports
goes red with it, because the mutant serves the real artefacts a resolved seam
has not yet been ruled able to serve.

Apply from the repo root: `git apply <this file>`; revert with `git apply -R`.

diff --git a/playgrounds/labs-nuxt/modules/scenarios/useClientEmails/scenario.ts b/playgrounds/labs-nuxt/modules/scenarios/useClientEmails/scenario.ts
--- a/playgrounds/labs-nuxt/modules/scenarios/useClientEmails/scenario.ts
+++ b/playgrounds/labs-nuxt/modules/scenarios/useClientEmails/scenario.ts
@@ -18,7 +18,8 @@
   useClientEmailManager,
   useClientEmails
 } from "@upmind-automation/headless";
-import { featureText, stepCatalog } from "../runtime/force/corpus.source";
+import clientEmailFeature from "@upmind-automation/headless/testing/client-email/feature?raw";
+import { clientEmailsSteps } from "@upmind-automation/headless/testing/client-email/steps";
 import { CLIENT_EMAIL_SCENARIO } from "../useClientEmail/scenario";
 import {
   cardUischema,
@@ -66,8 +67,8 @@
   // through the `ESC6` seam — empty until that ruling lands, which yields zero
   // tracks and leaves the page Live-only.
   tracks: {
-    feature: featureText,
-    catalog: stepCatalog,
+    feature: clientEmailFeature,
+    catalog: clientEmailsSteps,
     // The two `@smoke` journeys plus the `@fe-2824` one the parity oracle turns
     // on — the staff track is also the only one declaring a foreign scope, so
     // the player's navigate-first rule is one click from the bar.
